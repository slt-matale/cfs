// Central place to point the frontend at your backend.
//
// - While testing locally: leave this as the localhost URL and run the
//   backend with `uvicorn backend.app:app --reload`.
// - Once deployed: change this to your Hugging Face Space URL, e.g.
//   "https://YOUR_USERNAME-customer-feedback-bot.hf.space"
const API_BASE_URL = "http://127.0.0.1:8000";
